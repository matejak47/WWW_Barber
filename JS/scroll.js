function scrollDownToForm() {
    var formSection = document.getElementById('formSection');
    formSection.scrollIntoView({ behavior: 'smooth' });
}
function scrollDownToCenik() {
    var formSection = document.getElementById('sluzby');
    formSection.scrollIntoView({ behavior: 'smooth' });
}
function scrollDownToInfo() {
    var formSection = document.getElementById('info');
    formSection.scrollIntoView({ behavior: 'smooth' });
}